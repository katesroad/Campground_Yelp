import * as React from 'react'
import AuthForm from '.'

const Template = () => <AuthForm />

export const Login = Template.bind({})

export default {
  title: 'components/AuthForm',
  component: AuthForm,
}
